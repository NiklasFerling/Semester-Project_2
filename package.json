{
  "name": "semester-project_2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass --output-style compressed src/scss -o dist/css",
    "watch": "nodemon -e scss -x \"npm run build\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.2"
  },
  "devDependencies": {
    "node-sass": "^9.0.0",
    "nodemon": "^3.0.3"
  }
}
